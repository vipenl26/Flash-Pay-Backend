"use strict";

// importing required modules
const express = require("express");
const router = express.Router();
const Payments = require("../models/payments.js");

//default page that opens when user uses this route
router.get("/", (req, res) => {
    
});

